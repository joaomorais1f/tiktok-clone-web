import styled from 'styled-components';

const Input = styled.div`

`;

Input.Form = styled.form`

`;

Input.Text = styled.input`

`;

Input.Bar = styled.span`

`;

Input.Submit = styled.button`

`;

export default Input;